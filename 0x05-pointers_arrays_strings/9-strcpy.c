#include "main.h"

/**
 * _strcpy - copy a string 
 * @dest:destantion value 
 * @scr: source value 
 * Return:  the pointer to dest
 */
char *_strcpy(char *dest, char *src)
{
	int i;

	for (i = 0; scr[i] != '0'; i++)
	{
	dest[i] = src[i];
	}
	dest[1++] = '\0';
	return (dest);
}

